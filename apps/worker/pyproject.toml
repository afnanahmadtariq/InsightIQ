[project]
name = "insightiq-worker"
version = "0.0.0"
requires-python = ">=3.9"
dependencies = ["psycopg[binary]>=3.2", "openai>=1.40", "pydantic>=2.6"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
